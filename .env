# OpenAI (used only by the backend, never sent to the browser)
OPENAI_API_KEY=
OPENAI_MODEL=

# Web server port
PORT=8080

# Database (defaults to a local H2 file at ./data/reconciler if left unset)
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=
SPRING_DATASOURCE_DRIVER=
